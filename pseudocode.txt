This file is here simply to organize the flow of thoughts for how the extension will work


background.ts
        On startup:
          -Load tree from storage

        On tab create:
          -Create new tree node for the tab
          -Place the node in the tree in the proper spot
        
        Moving tab in browser: #Sidenote include moves in undo functions?
          -Get current windows tabs
          -find closest active tab and place the tab after that tab in the tree

sidepanel - App.tsx (should probably rename this to be sidepanel_app.tsx)
        On tab create: 
          -update view of tree
        Load suspended tab:
          -find closest ancestor active tab and open in index after
          -update tree view in sidepanel and popout depending on whos open

        Suspend tab:
          -Mark tab unactive and keep it in tree
          -close tab
          -update tree view in sidepanel and popout depending on whos open (use useEffect on tree object?)

        Delete tab: #Sidenote for future: save the deleted tabs for an undo function
          -Remove tab from tree
          -close tab
          -update tree view in sidepanel and popout depending on whos open

        Moving tab in tree view:
          -if tab is active
            -find closest active tab and place the tab after that tab in the browser
            -place the tab in the tree at the spot
          -if tab is inactive
            -place the tab in the tree at the spot
          

popout - App.tsx (should probably rename this to be popout_app.tsx)
        On tab create: 
          -update view of tree
        Load suspended tab:
          -find closest ancestor active tab and open in index after
          -update tree view in sidepanel and popout depending on whos open

        Suspend tab:
          -Mark tab unactive and keep it in tree
          -close tab
          -update tree view in sidepanel and popout depending on whos open (use useEffect on tree object?)

        Delete tab: #Sidenote for future: save the deleted tabs for an undo function
          -Remove tab from tree
          -close tab
          -update tree view in sidepanel and popout depending on whos open

        Moving tab in tree view:
          -if tab is active
            -find closest active tab and place the tab after that tab in the browser
            -place the tab in the tree at the spot
          -if tab is inactive
            -place the tab in the tree at the spot
